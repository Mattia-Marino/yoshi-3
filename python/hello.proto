syntax = "proto3";

service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply) {}
}

message HelloRequest {
  string data = 1; // JSON dei dati
}

message HelloReply {
  string message = 1; // JSON delle metriche
}